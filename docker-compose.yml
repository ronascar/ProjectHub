version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=${PORT}
      - NODE_ENV=${NODE_ENV}
      - JWT_SECRET=${JWT_SECRET}
      - FRONTEND_URL=${FRONTEND_URL}
      - VITE_API_URL=${VITE_API_URL}
    networks:
      - coolify
      - bgcwg4k8ckk0skss4w0kgcso
    restart: unless-stopped

networks:
  coolify:
    external: true
  bgcwg4k8ckk0skss4w0kgcso:
    external: true
